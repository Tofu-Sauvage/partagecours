<nav class="navbar navbar-expand-lg custom-navbar">
  <div class="container">
    <a class="navbar-brand" href="{{ path('home') }}"><i class="fas fa-dog"></i> Blog !</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link {{'#' in  app.request.uri ? 'active':'' }}" href="{{path('articles') }}">Articles</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{'#' in  app.request.uri ? 'active':'' }}" href="{{path('form_article') }}">Ajouter un article</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{'#' in  app.request.uri ? 'active':'' }}" href="#">Admin</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
