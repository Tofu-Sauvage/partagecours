{% extends './base.html.twig'%}{% block title %} | accueil{% endblock %} {%
block body %}
<div class="container mt-5">
  <h1>{{ article.title }}</h1>
  <p class="lead">
    {{ article.createdAt | date("d/m/Y")}} à
    {{ article.createdAt | date("H:i:s")}}
  </p>
  <div class="row mt-5">
    <div class="col-md-6 col-sm-12">
      {{ article.content | raw }}
      <br />
      <p class="text-end"><i class="fas fa-bone"></i></p>
    </div>
    <div class="col-md-6 col-sm-12">
      <img
        src="{{ asset('uploads/images/' ~ article.image) }}"
        class="shadow-lg p-3 mb-5 bg-body rounded"
        alt="..."
        style="width: 100%"
      />
    </div>
  </div>
</div>
{% endblock body %}
